export interface Project {
  title: string;
  slug?: string;
  shortDesc: string;
  longDesc: string;
  tags: string[];
  demoURL: string;
  thumbURL: string;
  githubURL: string;
  contextURLs: string[];
}
